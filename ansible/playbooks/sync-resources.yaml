---
- name: Sync cluster node resources
  hosts:
    - masters
    - workers
  become: true
  tasks:
    - name: Sync Resources
      ansible.builtin.include_role:
        name: master
        tasks_from: sync.yaml


